{% extends 'layout.html.twig' %}

{% block content %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}
    <a href="{{ path('frontpage') }}" class="btn btn-default"><i class="fa fa-home"></i> {{ "menu.back"|trans({}, 'menu') }}</a>
{% endblock %}
