{% trans_default_domain 'element' %}
{% extends 'layout.html.twig' %}
{% import '::macros.html.twig' as m %}

{% block content %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ m.start_button }}
        {% if not new %}
            {{ m.submit_button('submit', 'form.submit'|trans, 'check') }}
            {{ m.link_button('', 'form.delete'|trans, 'trash', 'btn-danger') }}
        {% else %}
            {{ m.submit_button('submit', 'form.create'|trans, 'plus') }}
        {% endif %}
        {{ m.end_button }}
    {{ form_end(form) }}
    {{ m.link_button(path('admin_enumeration', {'enumeration': element.enumeration.id}), 'form.back'|trans, 'arrow-left', 'btn-default') }}
{% endblock %}
